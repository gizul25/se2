Program is compiled and ready to launch.

    Extract first then run it via batch file on windows.
    Linux: the .dll file is in ./bin/Debug/net10.0/SE2.dll
    compiled just for win-x64 and linux-x64
